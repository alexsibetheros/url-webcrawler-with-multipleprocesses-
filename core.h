#ifndef corehead
#define corehead

#include "list.h"
#include "queue.h"

////////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////////////////////////////////
typedef struct core CORE;
typedef struct core* core_point;
struct core{
		int table_size;
		int total;
		int max;
		l_node_point * hash_table;
		p_queue queue;
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////////////////////////////////////////////////////////////////////////////////////
core_point initiate_core(int t_size,int m_size);
////////////////////////////////////////////////////////////////////////////////////////////////////////////
void destroy_core(core_point my_core);
////////////////////////////////////////////////////////////////////////////////////////////////////////////
void add_url(core_point my_core,char* get_url);
////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif /*corehead*/


