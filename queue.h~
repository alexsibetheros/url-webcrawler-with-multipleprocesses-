#ifndef _Queue_h
#define _Queue_h

typedef struct {
    char* url;
    struct queue *link;
} queue;
typedef queue * p_queue;


p_queue AddItem (p_queue listpointer, char* get_url);
p_queue RemoveItem (p_queue listpointer);
void PrintQueue (p_queue listpointer);
void ClearQueue (p_queue listpointer);
char* pop_top(p_queue *q_pointer);

#endif  /* _Queue_h */
